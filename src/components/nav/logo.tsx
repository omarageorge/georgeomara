import Link from 'next/link';

const Logo = () => (
  <div className=''>
    <Link href='/' className='btn btn-ghost font-semibold text-xl overlay-pane'>
      George &lt;&#47;&gt;
    </Link>
  </div>
);

export default Logo;
